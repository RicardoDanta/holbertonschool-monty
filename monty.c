#include "monty.h"
/**
 * main - LIFO FIFO
 * @argcounter: Something
 * @arglist: Something
 */
int main(int argcounter, char *arglist)
{
	FILE *file;

	file = fopen(arglist[1], "r");
	return (EXIT_SUCCESS);
}
